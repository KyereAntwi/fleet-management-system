import {Card} from "@chakra-ui/react";

interface Props {
    workspaceId: string;
}

const VehiclesDueForRoadworthyRenewalReportWidget = ({workspaceId}: Props) => {
    return (
        <>
            <Card w={'full'}>
            </Card>
        </>
    )
};

export default VehiclesDueForRoadworthyRenewalReportWidget;